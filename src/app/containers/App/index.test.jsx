import { expect } from 'chai';

describe('<App />', () => {
  it('Sample Success Test', () => {
    expect(true).to.equal(true);
  });
  it('Sample Failed Test', () => {
    expect(true).to.equal(false);
  });
});
